package main

import (
	hh_go_exam "Practicing_go/Misc_samples/HH_Go_exam"
)

func main() {
	hh_go_exam.AlaMainT3b()
	// HH_Go_exam.Task2()
	// learnsyncpoolusage.LearnSyncPoolUsage()
	// fmt.Println("Hey!")
	// wildberries.WbTechInterviewTask()
	// mySlice := make([]int, 0)
	// for i := range 4 {
	// 	mySlice = append(mySlice, i)
	// 	fmt.Println(mySlice, len(mySlice), cap(mySlice))
	// }
	// mySubSlice := mySlice[1:3:3]
	// fmt.Println(mySubSlice,len(mySubSlice),cap(mySubSlice))

	// // mySlice = slices.Delete(mySlice, len(mySlice)-2, len(mySlice)-1)

	// // fmt.Println(mySlice, len(mySlice), cap(mySlice))
	// // const mapSize = 5
	// // myMap := make(map[int]int)
	// // for i := range mapSize {
	// // 	myMap[i] = mapSize - i
	// // }
	// // fmt.Println(myMap)

	// // for {
	// // 	key := rand.IntN(mapSize * 2)
	// // 	fmt.Println("Key=", key)
	// // 	if _, ok := myMap[key]; ok {
	// // 		fmt.Println("Oh, this key is already exist")
	// // 		break
	// // 	}
	// // }

	// myString := "Привет!"
	// fmt.Println(myString)
	// myRunes := []rune(myString)
	// myBytes := []byte(myString)
	// for i := range myRunes {
	// 	fmt.Println(i, myRunes[i], string(myRunes[i]), "----", myBytes[i])
	// }
}
